# ============================================================
#  Copier ce fichier dans /etc/nginx/conf.d/websocket-map.conf
#  (doit être dans le contexte http{} de nginx.conf principal)
# ============================================================
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
